// const express = require('express');
// const router = express.Router();
// const multer = require('multer');
// const upload = multer({ dest: 'uploads/' });
// const houseImageController = require('../controllers/houseImageController');

// // House image routes
// router.post('/houses/:houseId/upload-image', upload.single('image'), houseImageController.uploadHouseImage);
// router.get('/houses/:houseId/images', houseImageController.getHouseImages);
// router.delete('/houses/:houseId/images/:imageId', houseImageController.deleteHouseImage);
// router.put('/houses/:houseId/images/:imageId', upload.single('image'), houseImageController.updateHouseImage);

// module.exports = router;